<%= form_for(@membership) do |f| %>
  <% if @membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
      <% @membership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
    <% end %>

    <% exclude = @beer_clubs.where(':user_id != ?', current_user.id) %>


    <%= f.select :beer_club_id, options_from_collection_for_select(@beer_clubs, :id, :to_s) %>
    <%= f.submit %>


 <!--
  <div class="field">
    <%= f.label :beer_club_id %><br>
    <%= f.number_field :beer_club_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
    @beer_clubs.where(@beerclub.arel_table[:user_id].not_eq(current_user.id))
 -->
<% end %>
