<table class="table table-hover">
  <thead>
  <tr>
    <th></th>
    <th>average</th>
  </tr>
  </thead>

  <tbody>
  <% collection.each do |item| %>
      <tr>
        <td><%= link_to item.name, item %></td>
        <td><%= sprintf('%.1f', item.average_rating) %> </td>

        <td></td>
      </tr>
  <% end %>
  </tbody>
</table>